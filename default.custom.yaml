patch:
  menu:
    page_size: 8
  
  schema_list:
    - schema: rime_ice               # 雾凇拼音（全拼）
    - schema: double_pinyin_flypy    # 小鹤双拼
    - schema: japanese               # 日文
    - schema: frost                  # 霜
    - schema: udonese                # 乌冬语

  key_binder:
    bindings:
      - { when: always, accept: Control+F1, select: .next }